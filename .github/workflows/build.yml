name: Build

on:
  push:
    branches:
      - prod
  pull_request:
    branches:
      - prod

env:
  CI: true

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest

    steps:
      - name: Clone Repository
        uses: actions/checkout@v3

      - name: Set Node.js Version
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Access EnviEndoc Directory
        run: ls ./EnviEndoc/EnviEndoc/

      - name: Install Dependencies
        run: npm install

      - name: Build
        run: npm run build
